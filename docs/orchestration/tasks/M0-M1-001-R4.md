# M0-M1-001-R4：两条并发缝隙与 cutoff 证据类型收口

状态：**DELIVERED — CODEX `ACCEPTED`**
Prompt 版本：`M0-M1-001-R4/v1`
Orchestrator：Codex
实现者：Claude
产品负责人/转发者：用户
预期基线：`feat/s2-alert-delivery@b450ed0`

## A. 修复任务包

### A1. R3 验收结论与本轮目标

R3 已正确完成并通过独立验证的部分：absent 0/0 committed shape、已进入 `stopping` 的 tombstone
保留、failure stamp、真实 Start selected / Apply policy 单 basis、lost START/STOP 撤绿与 identity 保留、
CLOSED/ERROR 全局 cutoff、严格 Windows policy 比较和 test-only checkpoint seam。最终 12 文件 Windows
archive 也真实通过了非空物化、重启、Full Reset 与再次空重启。

Codex 对 `2cd54e2..b450ed0` 的结论仍为 `CHANGES_REQUIRED`。只剩两条确定性并发缝隙和一条
证据类型合同差异。本轮不得重写已经通过的机制，不进入 M2。

### A2. 基线与工作树

- 在 `feat/s2-alert-delivery@b450ed0` 上追加提交；不得 reset、rebase 或改写旧提交；
- Codex 会留下以下 orchestration 修改，Claude 必须原样提交，不改写裁定：
  - `docs/orchestration/CURRENT.md`；
  - `docs/orchestration/tasks/M0-M1-001.md`；
  - `docs/orchestration/tasks/M0-M1-001-R3.md`；
  - 本文件；
- 两个 `docs/conversations/2026-08-23-*` 目录继续未跟踪，不提交、不移动、不删除；
- 除此之外若有未知修改，停止并精确回报；
- 不合并、不 tag、不发布、不覆盖 `release/0.1.0`。

### A3. P1：rotation 必须按物理 live truth 保留 tombstone

R3 的 `rotate_if_due()` 只从 `materialization.sections[*].stopping` 构造 preserve set。它没有覆盖
“STOP 已经在 SQLite 提交、但尚未取得 materialization lock 做 reconcile”的合法中间态：

1. section 已经物理 armed，receipt/tombstone budget 已到 rotation threshold；
2. rotation 持有 materialization lock；
3. 并发 STOP 在 store 中提交 tombstone，然后阻塞在 reconcile 前；
4. materialization 仍把该 section 记为 `armed`，所以 R3 preserve set 不含它；
5. rotation 删除刚提交的 tombstone；
6. rotation reconcile 尝试 stop，owner stop 恰好失败，于是内部留下 `stopping` id；
7. GET 只投影 authority rows，物理 watch 仍活却再次无 row、无 STOP 控制。

注意：仅把 store guard 从 budget read 持有到 rotation **不能**完整修复，因为 STOP 可以在 rotation
开始前就已经提交、只是在 reconcile 前暂停。

必须修成：

- tombstone 是否仍承担物理 teardown identity，由 owner 的当前 live-watch truth 决定，而不是只看
  materialization 已经来得及切换到哪个标签；
- 推荐在持有 materialization lock 时，从 `owner.watch_targets()` / `live_watches(owner)` 的 section keys
  构造 `preserve_tombstones`，再交给 store rotation；等价方案必须证明覆盖 committed-before-reconcile；
- live snapshot 之后 watch 消失而多保留一条 tombstone是安全的；仍活 watch 被漏掉不是；
- 不得把所有 tombstone 永久保留，也不得扩大 authority 的 9 条 desired cap；
- STOP fault 后必须保留 `policy = null` row、`pendingDisarm = true`、原 physical id 可寻址，且 response
  不能使用“合法已回收”的 absent 0/0 shape；fault 解除后仍按原 id 清理。

最低判别测试必须无 sleep，并建立如下真实顺序：rotation 到 `RotationDue` → 并发 STOP 已完成 store
commit、到 `MutationDecided`/reconcile 前 → rotation 继续 → owner stop fault。当前 `b450ed0` 必须失败；
修复后断言 row 仍在、pending、physical id 仍 live/addressable，随后解除 fault 可完成 teardown。

### A4. P1：mutation uncertainty 的同步权威只能有一个

`markUncertain()` 同步写 `uncertainAt.current`，但生产提交门和 removal 门仍读
`uncertainKeysRef.current`；后者只由 React `useEffect` 在下一轮 render 后同步。`b450ed0` 的提交说明
声称已经改读同步 map，但该提交实际只改测试/文档，没有改 production file。

必须修成：

- 从 PUT 真正发出的同一时刻起，同 section 的第二手势与 remove 都直接读取同步权威；
- 优先让 `submitAgainst` 与 `isRemovable` 直接检查 `uncertainAt.current.has(identity)`，并删除不再需要的
  effect-lagged mirror；等价方案必须在 `markUncertain` 内同步更新唯一门控 ref；
- 不能依赖下一次 render/effect、按钮重渲染或服务端 CAS 才拒绝；
- settle 只能继续由同 sequence 或更晚的成功 authority answer 清除；不能自动重放。

判别测试必须在**同一个 `act` 回调**中：让首个 PUT 已被 fetch mock 观察且保持 pending，不退出 `act`
就立即发第二手势并调用 remove。必须本地拒绝、selection/管理 identity 不消失、PUT 始终只有 1 次；
随后首个 answer/recovery read 落地才恢复操作。现有“第一个 act 完成后再点第二次”的测试保留，但不能替代它。

### A5. P2：只有 OPEN 后的新 GET 可以解除 socket cutoff

R3 A7 冻结的是：CLOSED/ERROR 后，只有 OPEN 之后新 issued 且成功返回的 GET 才能解除全局 cutoff。
当前 `applyAuthority()` 不知道 answer 来源，因而 PUT 的 COMMITTED snapshot 也能解除。

必须修成：

- 给 authority answer 标记证据来源，或只在 `refreshIntent` 的成功 read path 执行 cutoff release；
- mutation response 可以更新 authority snapshot、settle uncertainty，但不得单独恢复 green/active；
- OPEN 本身也不得解除；必须等 OPEN 后的新 GET 成功；
- section-specific disproof 与已有 CLOSED/ERROR × null/PREPARING 四例保持不变。

判别测试：先建立 cutoff；在同一个 `act` 中切到 OPEN，并在 reconnect effect 的 GET 排队前让一个 PUT
返回 RUNNING snapshot。此时仍不得绿色；随后 OPEN 后的新 GET 成功才恢复。回退 evidence-kind 控制时
该测试必须失败。

### A6. 已通过内容与明确不扩展

以下内容只做回归，不重新设计：

- absent 0/0 shape 对任何“提交后又被合法并发 caller supersede”的 COMMITTED current-state 都可成立，
  不得错误限制为原始 STOP 才能 decode；
- failure stamp、stale retry、真实 batch 单 basis、trusted identity、全局 cutoff 的失效半边、严格 policy
  comparator、test-only checkpoints 均保持；
- Claude 披露的“recovery GET 挂起时其他 section 仍使用旧 snapshot”暂作为非阻断限制：这些 PUT 被同一
  authority queue 挡在 GET 后，rotation 时旧 generation 会由 CAS 拒绝，不能静默覆盖；
- 不做自动重连/P1 换票、完整 Readiness、AudioContext 自愈、页面 Notification、L2/L3、H4/完整 P2、
  S3/S4、N1–N3；不复活 desired WS、leader/BroadcastChannel。

### A7. 验证与速度纪律

开发中只跑三个 finding 的 focused tests。全部完成后集中跑一次：

```text
cargo test --workspace
node tools/architecture/verify-rust-graph.mjs
node tools/architecture/verify-public-rust-zero-surface.mjs
cd frontend && npm run verify
git diff --check b450ed0..<head>
git diff --check 2cd54e2..<head>
git diff --check a4f8d22..<head>
clean detached final head Windows archive build
严格 policy 的非空→重启 attach/materialize→reset→再重启 E2E
```

使用仓库已有锁定工具，不联网安装，不访问真实 Rutgers，不覆盖旧 archive/release。只构建一次 final
archive；报告路径、SHA、sourceCommit、sourceDateEpoch、12 files 和 verifier JSON。

### A8. 回报格式

一次性返回：Outcome；Commits；A3–A5 closure 与判别测试；Changed files；合同决定；focused + full
tests；negative proof；archive 证据；known limitations；git status；`b450ed0..<head>`、
`2cd54e2..<head>`、`a4f8d22..<head>`。不要自行宣布 S2/L1 accepted。

## B. Claude Prompt（请用户从下一行开始原样复制）

你正在 `Z:\Project\Rutgers-BetterCourseSchedulePlanner` 继续实现 RBCSP。Codex 是 orchestrator/最终
验收者，你是唯一产品代码实现者。请在当前 `feat/s2-alert-delivery@b450ed0` 上一次性完成
`M0-M1-001-R4/v1`，不要逐 finding 停下来等待。

先完整阅读并遵守：

1. `docs/orchestration/CURRENT.md`；
2. `docs/orchestration/tasks/M0-M1-001-R3.md` 末尾 Codex R3 验收记录；
3. `docs/orchestration/tasks/M0-M1-001-R4.md` 的 A 部分。

冲突优先级：用户最新裁定 > CURRENT > R4 A 部分 > R3 > R2 > 原任务 > 旧设计。

不得 reset/rebase/改写旧提交。Codex 已知的工作树修改只有 CURRENT、原任务、R3、R4 四份
orchestration 文档，以及两个必须保持未跟踪的 conversation 目录。四份文档原样提交，不修改裁定；
conversation 目录不提交、不移动、不删除。除此之外若有未知修改，停止并精确回报。

本轮只关闭三个残余，不进入 M2：

1. 修复 STOP committed-before-reconcile × rotation：rotation 的 tombstone preservation 必须来自 owner 的
   当前 physical live-watch keys（或能严格证明等价的机制），不能只看已经进入 `stopping` 的记录，也不能
   只靠扩大 budget-read/store-lock 区间。用 `RotationDue` 与 `MutationDecided` 建立无 sleep 交错：STOP
   已提交、materialization 尚为 armed、rotation 继续、owner stop fault。最终必须保留 tombstone row、
   pendingDisarm 和原 id；解除 fault 后按原 id 清理。
2. 修复 mutation uncertainty 同步门：`submitAgainst` 和 `isRemovable` 必须读取 `markUncertain` 同步写入的
   `uncertainAt.current`（或同一同步权威），不再读取下一轮 effect 才更新的 mirror。新增同一个 `act`
   内“首 PUT 已发出但 effect 未 flush → 第二手势 + remove”的测试；只有 1 次 PUT，row/selection 不丢。
3. 把 socket cutoff 的解除限定为 OPEN 后新 issued 的成功 GET。PUT snapshot 可更新 authority/settle
   section，但不能解除 cutoff。新增 OPEN 与 reconnect effect 之间先落 PUT 的判别测试，直到 GET 成功
   都不得复绿。

保持 absent 0/0、failure stamp、真实 batch、trusted identity、现有 cutoff 矩阵、严格 Windows policy、
test-only seam 和三生命周期 packaging 行为；不要重写已通过设计。recovery GET 挂起时其他 section 的
旧-basis 请求排在同一 queue 后且受 CAS 拒绝，继续作为已披露非阻断限制，不在本轮扩展。

开发中只跑 focused tests；全部完成后集中跑一次 workspace、frontend、两项 architecture、三个
diff-check，再从 clean detached final head 只构建一次新 Windows archive 并跑严格 policy 三生命周期
E2E。使用已有锁定工具，不联网安装、不访问真实 Rutgers、不覆盖旧 release/archive。

不做自动重连/P1 客户端、完整 Readiness、音频自愈、Notification、L2/L3、H4/完整 P2、S3/S4、
N1–N3；不复活 desired WS/leader/BroadcastChannel；不合并、不 tag、不发布。完成后按 R4 A8 一次性
回报并给三个 review range。S2/L1 是否接受由 Codex 独立验收决定。

## C. Codex R4 独立验收记录（2026-08-24）

- Claude 交付 head：`75cefb0a27b85495d9b5a6e2c7f93b5590b3ee95`；
- R4 范围：`b450ed0..75cefb0`；全里程碑：`a4f8d22..75cefb0`；
- 结论：`ACCEPTED`；`M0-M1-001`、reduced S2-D3 与 L1 本地 desired 纵向闭环完成；
- A3 通过：rotation 在 materialization 排他域内以 owner live section keys ∪ conservative stopping
  records 构造 preserve set；新单元测试把 STOP 固定在 store commit 后、reconcile 前，并证明 rotation
  窗口内原 id 仍 live、teardown 调用为零，因此旧 stopping-only 实现会确定性删错 row；
- A4 通过：提交与 removal 直接读 `uncertainAt.current`；同一 `act` 内首 PUT 已发出、render 未发生时，
  第二手势与 Remove 都被拒，selection/管理 row 保留且 PUT 只有一次；
- A5 通过：authority answer 显式携带 `READ | MUTATION` evidence；PUT 可更新 snapshot、settle section，
  只有 OPEN baseline 之后的新成功 GET 能解除 socket cutoff；
- Codex 独立复跑：`cargo test -p bcsp-local-runtime --test local_runtime` 26/26、workspace 746 passed /
  1 ignored、frontend 254 passed、两项 architecture 与三个 diff-check 全绿；
- 最终 Windows archive 独立复验通过：12 files，SHA-256
  `c1a87c3f5954bb20ec336fa713178a7ffa5efa3b469428adc417a5163a361845`，sourceCommit
  `75cefb0a27b85495d9b5a6e2c7f93b5590b3ee95`，sourceDateEpoch `1787598849`，两次重启、非空物化、
  Full Reset 与再重启为空均成立；
- “构建两次”获显式豁免：第一次来自 `bdb0772`，随后纯测试 assertion 文本提交使 head 前移，Claude
  重新构建最终 head；只覆盖本轮 disposable `.cache` 输出，没有覆盖正式 release/R3 证据，最终候选
  provenance 完整，因此不要求第三次构建；
- 首轮 workspace 的 3 个 HTTP integration failure 没有保留完整日志，不能声称原因已诊断。Codex 与
  独立审查随后复跑完整 workspace、单独 local_runtime 及高并发重复均未复现；本轮 production Rust
  改动只在 409 tombstones / 1638 receipts 的 rotation 门后可达，而三例失败两例与 desired 无关、另一例
  只做个位数 mutation。记录为未解释 transient 稳定性观察项，不阻断本轮；若再次出现须保留精确
  response/status/log 并重新定级；
- 当前 branch 未合并、未 tag、未发布；两个 conversation 目录保持未跟踪。

`ACCEPTED` 仅表示 M0/M1 完成，不表示整个 S2 或项目完成。验收当时的下一里程碑曾拆为 M2/M3；
用户随后在 M2 尚未实施前改用 Stage 级推进。当前以 `STAGE-2/v1` 一次完成原 M2/M3/M4。
