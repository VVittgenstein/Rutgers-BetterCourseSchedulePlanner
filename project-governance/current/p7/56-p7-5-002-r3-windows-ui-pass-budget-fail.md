# P7.5-002-R3 - Windows Chrome UI pass, request-budget failure

- Parent: `d19a19e607e180b5ac1f42cbc5f246458fc4b7c5`
- Candidate: `eabdc3b5f4a705d8c22e6941831f55e0bb5b5c2a1c33e648e545f86007cab577`
- Started: `2026-07-15T13:35:18.1964817Z`
- Graceful exit requested: `2026-07-15T13:42:52.3837725Z`
- Live duration: `454.187` seconds

The exact 11-file Windows ZIP was extracted by a new disposable standard
Windows user. It contained no data directory or database. Direct launch of
`RBCSP.exe` from outside the package created the first database only at the
package-relative data path.

Chrome operated the real loopback page. The decisive product flow reached a
ready real Catalog, used three active filter dimensions with non-empty results,
opened real Course and Section details, and selected a current real Open Section
from the joined result. The page displayed refresh, freshness, lag, and count
evidence. A real browser WebSocket reached Connected with one active watch; the
selected item became Watching, the Open alert and toast appeared, and the
user-gesture-unlocked sound path was audible. The watch was stopped immediately,
the socket was disconnected, and a browser reload showed the closed episode in
local History. Saved views, Settings, and Reset were not repeated because they
are outside the bounded P7.5 live mandatory scope and already have deterministic
P7.2/P7.4 coverage.

The run nevertheless fails the approved request budget. Real discovery yielded
`N=15`, so Open was limited to `18` and total Rutgers requests to `35`. The
durable ledger recorded `2` discovery requests, `15` Catalog requests, and `23`
Open requests, for `40` total. Fourteen Open targets were attempted once and one
demanded target was attempted nine times. Open and total therefore each exceeded
the limit by five. No Rutgers `403` or `429` was observed.

The local exit endpoint returned `204` with 25.813 seconds remaining in the
480-second window. The process, listener, and instance lock disappeared;
SQLite integrity was `ok` and no WAL/SHM remained. The archive hash was unchanged.
The exact disposable user, profile, package, database, and raw Rutgers data were
removed, and no screenshot or raw response evidence was retained.

This candidate is retired under the one-run rule. The next work is a product-level
repair of repeated demanded-target Open scheduling, followed by a newly built
candidate; the failed candidate must not be rerun.

Gate: `P7_5_WINDOWS_BOUNDED_OPEN_REPAIR_REQUIRED`.
P7 completed: `false`.

