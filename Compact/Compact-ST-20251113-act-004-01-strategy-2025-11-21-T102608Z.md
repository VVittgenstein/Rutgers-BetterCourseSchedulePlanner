## Facts
- Discord strategy doc (`docs/discord_strategy.md`) corrected invite template to use OAuth bot install with permissions `19456` (VIEW_CHANNEL + SEND_MESSAGES + EMBED_LINKS) and scopes `bot applications.commands`; still positions channel as default with DM opt-in, fallback-to-channel on DM failure, and rate-limit/backoff guidance.
- Example bot config (`configs/discord_bot.example.json`) now ships with the OAuth bot invite URL using the same permissions/scopes, retaining placeholders for IDs/tokens, DM verification settings, mention controls, rate-limit knobs, message templates, and test hooks.

## Interfaces / Behavior
- Deployment guidance now explicitly requires bot install via OAuth (not server-only invite) to ensure slash commands and embeds work; embed-capable permission sum is documented.

## Tests
- Not run (docs/config only).

## Risks / TODO
- Bot sending logic, rate-limit enforcement, and slash-command/DM verification remain unimplemented; config values are placeholders needing real IDs/secrets.

## Code Review - ST-20251113-act-004-01-strategy - 2025-11-21T10:27:25Z
---review-start---
configs/discord_bot.example.json


  "defaultMode": "channel",
  "defaultGuildId": "234567890123456789",
  "defaultChannelId": "345678901234567890",
  "inviteUrl": "https://discord.gg/your-server",


@chatgpt-codex-connector
chatgpt-codex-connector bot

P1 Badge Use OAuth bot invite instead of server join link

The example config’s inviteUrl points to https://discord.gg/your-server, which is a human invite to a server and does not authorize the bot with the bot/applications.commands scopes or requested permissions described elsewhere in the doc. Anyone copying this template will end up inviting users rather than installing the bot, so the bot cannot join guilds or expose slash commands. Provide the OAuth2 bot invite URL (with client_id, scopes, and permissions) to avoid misconfiguring deployments.
---review-end---

## Code Review - ST-20251113-act-004-01-strategy - 2025-11-21T10:32:58Z
---review-start---
Codex Review: Didn't find any major issues. Keep them coming!
---review-end---
