## Facts
- Added Discord strategy doc (`docs/discord_strategy.md`) defining MVP default as channel broadcast with DM opt-in, fallback to default channel on DM failure, required scopes (`bot`, `applications.commands`), invite template, message copy, and rate-limit/backoff guidance (global ≤20 msg/s, per-channel 5 msg/5s, jitter/backoff on 429).
- Created example bot config (`configs/discord_bot.example.json`) with env var placeholders (`DISCORD_BOT_TOKEN`, `DISCORD_PUBLIC_KEY`), default guild/channel IDs, DM verification command and timeout, optional role mention, rate-limit/backoff knobs, message templates, and testHooks (dryRun, overrides).
- Updated `record.json` subtask `ST-20251113-act-004-01-strategy` status to `done`, refreshed timestamps, and listed artifacts above.

## Interfaces / Behavior
- Default delivery mode is `channel`; DM requires slash-command verification and can fall back to a configured channel on privacy errors.
- Config surface now established for future sender: includes rate limit policy (global/per-channel), mention controls, and template tokens (`{{courseTitle}}`, `{{indexNumber}}`, `{{manageUrl}}`, etc.).

## Tests
- Not run (docs/config only).

## Risks / TODO
- Sending logic, rate-limit enforcement, and slash-command/DM verification flows are not yet implemented; config uses placeholder IDs/URLs.
