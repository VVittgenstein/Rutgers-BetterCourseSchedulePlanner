## 已落实事实
- 课程卡片底部新增订阅面板（`SubscribeButton`），包含班次快捷选择（展示 open/waitlist/closed）、Index 输入、邮箱/Discord ID 切换、退订 token 输入、重复点击冷却与本地存储联系人；反馈区显示成功/重复/需要验证/未解析 section/traceId 信息。
- 前端 API 层增加 `apiPost` 与 `/api/subscribe`、`/api/unsubscribe` 封装，扩充订阅请求/响应类型定义；i18n 增补 en/zh 文案覆盖订阅流。
- 列表行高度调整以容纳订阅面板；section 预览转换去除无效 meeting，FilterPanel 动态 i18n key 放宽以通过编译。

## 自测结果
- `cd frontend && npm run build`
- 本地 SQLite 种子（term=2024FA、campus=NB、course 01:198:111、section 12345），通过 Fastify `app.inject`：`POST /api/subscribe` 得到 201（includes unsubscribeToken），随后 `POST /api/unsubscribe` 得到 200（previousStatus=pending）。

## 风险 / TODO / 外部依赖
- 需保证运行时数据库存在目标 term/campus/section，否则订阅返回 sectionResolved=false（UX 已提示但未自动回填）。
- 后端独立启动未验证（另行配置端口/路径）；依赖 SQLite 本地数据可用。***
