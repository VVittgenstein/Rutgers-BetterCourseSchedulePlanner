{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rbcsp.invalid/schema/bcsp-public-env-v1.json",
  "title": "RBCSP public service environment",
  "type": "object",
  "additionalProperties": false,
  "required": ["BCSP_PUBLIC_ORIGIN"],
  "properties": {
    "BCSP_PUBLIC_ORIGIN": {
      "type": "string",
      "format": "uri",
      "pattern": "^https://[^@/?#\\s]+$",
      "description": "External HTTPS origin without credentials, a path, query, or fragment."
    },
    "BCSP_PUBLIC_WS_PER_CLIENT_LIMIT": {
      "type": "string",
      "pattern": "^(102[0-4]|10[0-1][0-9]|[1-9][0-9]{0,2})$",
      "description": "Optional per-client concurrent WebSocket cap, 1..1024 (default 64). The only tunable capacity number; the service refuses to start on an out-of-range value."
    }
  },
  "secretVariables": []
}
